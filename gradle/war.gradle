apply plugin: "war"

bootWar {
    mainClassName = "com.cours.MicroserviceEmployeApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
